export function focusZone({e}){
    // what's going on here?? buddy? is this mid attempt at something? don't f 
    // this up mother m'er 
    if(e.target.closest('.side-bar')){return 'sideBar'}
    if (e.target.closest('.side-bar')) { return 'sideBar' }

}